<?php
declare(strict_types=1);

/**
 * Copy to mail.local.php on the server (not committed to git).
 * ZeptoMail → Mail Agent → SMTP → Send Mail token.
 */
return [
    'smtp_password' => 'your-zeptomail-send-mail-token',
];
